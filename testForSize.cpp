/*
 * @Author: LetMeFly
 * @Date: 2022-03-18 11:11:03
 * @LastEditors: LetMeFly
 * @LastEditTime: 2022-03-18 11:12:42
 */
#include <bits/stdc++.h>
using namespace std;
#define mem(a) memset(a, 0, sizeof(a))
#define dbg(x) cout << #x << " = " << x << endl
#define fi(i, l, r) for (int i = l; i < r; i++)
#define cd(a) scanf("%d", &a)
typedef long long ll;
int main() {
    int Int;
    short int SInt;
    uint8_t UInt8;
    dbg(sizeof(Int));
    dbg(sizeof(SInt));
    dbg(sizeof(UInt8));

    return 0;
}

/** 运行结果：
sizeof(Int) = 4
sizeof(SInt) = 2
sizeof(UInt8) = 1
*/